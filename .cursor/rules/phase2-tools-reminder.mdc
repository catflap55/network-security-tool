---
description: Keep Security Console defensive and localhost-first
alwaysApply: true
---

# Security Console

Prefer defensive checks (ports, TLS, headers), localhost bind, console token, and authorized targets only.

Do not add credential attacks, exploit payloads, or default secrets.
