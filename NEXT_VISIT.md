# Next work

Shipped: TLS inspect, HTTP headers, job diff, local schedules, console token, target limits.

Still optional later (do not bundle exploits):

- Operator-supplied Nuclei templates
- OpenVAS/Greenbone as a separate documented docker compose (Phase 3)
